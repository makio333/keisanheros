# 💰 クレジット節約ガイド（トークン・コスト削減ルール）

このドキュメントは、AIアシスタント（Claude Code / Antigravity / ChatGPT等）が開発を行う際に、**クレジット（トークン・API費用・推論コスト）を無駄に消費する原因**と、**それを防ぐための具体的な解決策・運用ルール**をまとめたものです。

**【重要ルール】**
開発・改修・アップデートに着手する際は、**必ずこのファイルを最初に参照し、ルールを厳守して作業してください**。

---

## 🚨 クレジットを大量消費する5大原因と解決策

### 1. 巨大ファイルの全件読み込み（View File / Read Entire File）
- **原因**:
  - `game.js`（約6,000行）や `GAME_DETAILS.md`（約1,600行）などの巨大ファイルを、行数指定なしでファイル丸ごと読み込むと、1回のツール呼び出しで数万〜十数万トークンを消費する。
- **解決策**:
  - **行範囲指定の徹底**: `grep` で該当箇所を特定した後、必要な範囲（例: `StartLine: 100, EndLine: 150`）だけをピンポイントで読み込む。
  - **無駄な全体閲覧の禁止**: 概要把握は目次やgrepを活用し、ファイル全体を一度に読み込まない。

---

### 2. 巨大ファイルの全体書き換え（Full Overwrite）
- **原因**:
  - わずか数行の修正のために、ファイル全体を `write_to_file` で丸ごと上書き送信すると、送受信トークンが膨大になりクレジットが激しく消費される。
- **解決策**:
  - **差分置換ツールの使用**: `replace_file_content` / `multi_replace_file_content` を使い、変更箇所のみを局所的に置換する。
  - **新規作成時のみ上書き**: ファイル全体の書き込みは、完全新規ファイルの作成時のみに限定する。

---

### 3. 広範囲・無計画なファイル検索・探索
- **原因**:
  - 検索パスを絞らずにプロジェクト全体を再帰検索したり、結果が何百件も出るキーワードで grep すると、出力ログでトークンが急増する。
- **解決策**:
  - **対象ディレクトリ・拡張子の絞り込み**: `SearchPath` や `Includes: ["*.js"]` などを指定して検索範囲を限定する。
  - **MatchPerLine / 件数上限**: 不要に行全体を大量取得せず、ファイル名のみ（`git grep -l` 相当）やピンポイントなキーワードで検索する。

---

### 4. 冗長なシミュレーション出力・長大ログの垂れ流し
- **原因**:
  - 検証用スクリプトで1万回のループログや全オブジェクトの中身を `console.log` で垂れ流すと、ツール実行結果のトークンが爆発する。
- **解決策**:
  - **ログの集約**: シミュレーション結果は「最終勝率（%）」や「エラー件数」など、1〜数行のサマリー形式に集約して出力する。
  - **デバッグログの消去**: 本番コードや検証スクリプトに不要な詳細ログを残さない。

---

### 5. 文脈（コンテキスト）の肥大化と不要な往復
- **原因**:
  - 1つの指示に対して小さな確認を何度も往復したり、不要な長文解説を毎回繰り返すと、履歴（コンテキスト）が長くなり、毎ターンの消費トークンが指数関数的に増加する。
- **解決策**:
  - **1ターンでの的確な実行**: 調査・計画・修正・検証を無駄な中間会話を挟まずにスムーズに遂行する。
  - **簡潔な回答**: 結論、変更点、検証結果を箇条書きで短く報告する（不要な長文の繰り返しを避ける）。

---

## 📋 毎回の作業フロー（節約チェックリスト）

作業を開始するたびに以下の手順を遵守してください：

```
[1. 調査]
  └─ grep_search で対象関数・行番号を特定（ファイル全体を読まない）
  └─ view_file で必要な行数だけ（StartLine/EndLine指定）を閲覧

[2. 修正]
  └─ replace_file_content で対象箇所のみをピンポイント差分置換

[3. 検証]
  └─ 構文チェック（node -c 等）＆ 要約された出力のみのテストを実行

[4. ドキュメント更新]
  └─ GAME_DETAILS.md の該当箇所・履歴のみを replace_file_content で更新

[5. 完了報告]
  └─ 要点を簡潔に報告
```

---

## 📌 更新履歴
- **2026-08-22**: 初版策定。巨大ファイルの差分編集、行範囲読み込み、ログ集約、コンテキスト管理のルールを制定。
